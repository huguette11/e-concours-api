export class VerifyNumber{

    async ApiFetch ({apikey}){}
    
    async VerifiyN({number}){
        try{
            // verifier si c'est orange 

            const orange =  await fe

            //sinon verifier si c'est  moov

            // sinon verifier encore si c'est moov;

            // sinon mtn le numero n'est pas fonctionnelle

        }
        catch(err){

        }
    }
}